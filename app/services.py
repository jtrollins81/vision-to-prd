# This file will hold AI call logic later (OpenAI or other LLM).
# For now, just a placeholder so the structure is ready.

def generate_prd_stub(vision_input: dict) -> str:
    return f"PRD placeholder for: {vision_input.get('problem', 'Unknown Problem')}"
